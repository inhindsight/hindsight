apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hindsight.fullname" . }}-orchestrate
  labels:
    {{- include "hindsight.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.orchestrate.replicaCount }}
  selector:
    matchLabels:
      app: hindsight-orchestrate
      {{- include "hindsight.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: hindsight-orchestrate
        {{- include "hindsight.selectorLabels" . | nindent 8 }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ include "hindsight.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-orchestrate
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ template "hindsight.image" . }}
          command:
            - orchestrate/bin/orchestrate
            - start
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: metrics
              containerPort: 9568
              protocol: TCP
          env:
            - name: ORCHESTRATE_DB_HOST
              value: {{ template "postgres.fullname" . }}
            - name: ORCHESTRATE_DB_NAME
              value: {{ .Values.presto.view_state_apps.orchestrate.db.name }}
            - name: ORCHESTRATE_DB_USER
              value: {{ .Values.presto.view_state_apps.orchestrate.db.user | required "app db.user must be set" }}
            - name: ORCHESTRATE_DB_PASSWORD
              value: {{ .Values.presto.view_state_apps.orchestrate.db.password | required "app db.password must be set" }}
            - name: KAFKA_ENDPOINTS
              value: {{ template "hindsight.fullname" . }}-kafka-bootstrap:9092
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
