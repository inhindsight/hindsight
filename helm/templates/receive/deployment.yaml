apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hindsight.fullname" . }}-receive
  labels:
    {{- include "hindsight.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.receive.replicaCount }}
  selector:
    matchLabels:
      app: hindsight-receive
      {{- include "hindsight.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: hindsight-receive
        {{- include "hindsight.selectorLabels" . | nindent 8 }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      serviceAccountName: {{ include "hindsight.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-receive
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ template "hindsight.image" . }}
          command:
            - receive/bin/receive
            - start
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: websocket
              containerPort: 80
              protocol: TCP
            - name: udp-push
              containerPort: 6790
              protocol: UDP
            - name: metrics
              containerPort: 9568
              protocol: TCP
          env:
            - name: RECEIVE_DB_HOST
              value: {{ template "postgres.fullname" . }}
            - name: RECEIVE_DB_NAME
              value: {{ .Values.presto.view_state_apps.receive.db.name }}
            - name: RECEIVE_DB_USER
              value: {{ .Values.presto.view_state_apps.receive.db.user | required "app db.user must be set" }}
            - name: RECEIVE_DB_PASSWORD
              value: {{ .Values.presto.view_state_apps.receive.db.password | required "app db.password must be set" }}
            - name: KAFKA_ENDPOINTS
              value: {{ template "hindsight.fullname" . }}-kafka-bootstrap:9092
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
